"""E2E tests for container runtime operations.

These tests verify that Docker and Podman runtimes work correctly
with actual container operations. Tests gracefully skip when
the required runtime is not available.
"""
